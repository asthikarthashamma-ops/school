services:
  - type: web
    name: aura-academy-sms
    env: docker
    branch: main
    dockerfilePath: ./Dockerfile
    plan: starter
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "5000"
      - key: NODE_ENV
        value: production
